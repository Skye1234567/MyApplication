<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Admin_Menu"></activity>
        <activity android:name=".Sign_up_admin" ></activity>
        <activity android:name=".Set_Parameters"></activity>
        <activity android:name=".Set_Parameters_Practice" ></activity>
        <activity android:name=".Set_Parameters_Round_2"></activity>
        <activity android:name=".Wait_Page"></activity>
        <activity android:name=".MarketPlace"></activity>
        <activity android:name=".Manager_Instructions"></activity>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>